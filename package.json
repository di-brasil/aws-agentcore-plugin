{
  "name": "agentcore-assistant",
  "version": "4.4.0",
  "description": "MCP server that gives AI coding assistants accurate, always-fresh knowledge about Amazon Bedrock AgentCore. Indexes 1800+ pages from 13 live official AWS sources (developer guide, data/control plane APIs, boto3, Python SDK, CloudFormation, CDK for 5 languages, FAQs) with zero static content. All documentation fetched on demand with configurable local caching.",
  "main": "dist/index.js",
  "bin": {
    "agentcore-assistant": "./dist/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node18 --format=cjs --outfile=dist/index.js",
    "build:types": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "validate": "node scripts/validate-plugin.mjs",
    "test:watch": "vitest"
  },
  "keywords": [
    "mcp",
    "agentcore",
    "aws",
    "bedrock",
    "ai-agents",
    "documentation",
    "claude-code",
    "kiro",
    "cursor"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.24.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
